<view class="sort-row">
  <view
    wx:for="{{viewOptions}}"
    wx:key="id"
    class="{{item.className}}"
    data-id="{{item.id}}"
    bindtap="handleChange"
  >
    {{item.name}}
  </view>
</view>
